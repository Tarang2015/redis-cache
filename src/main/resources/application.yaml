server:
  port: 9090
spring:
  datasource:
    url: jdbc:h2:file:~/test
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2
  redis:
    host: localhost
    port: 6379

  cache:
    type: redis
jwt:
  secret: Abcke3457
